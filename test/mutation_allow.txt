# Mutants that no test can kill: file ## operator ## the source line ## why
src/engine.ml ## greater ## (s >:. data_bits) ## both arms of the saturation give data_bits when the sum is exactly data_bits
src/decoder.ml ## constant one ## if n = 1 lsl width raw then vdd else raw <:. n ## no enum checked here fills its field now that the jump condition is 12 of 16, so the branch is never built
src/engine.ml ## constant one ## let%hw decode_ok = reg spec ~enable:ir_load ~clear_to:vdd fetched.valid in ## the instruction register is always refilled between a start and the first issue, so the value after a clear is never used
